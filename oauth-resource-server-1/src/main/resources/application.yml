server:
  port: 8081
  servlet:
    context-path: /api-1
    
spring:
  application:
    name: api-1
    
eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8084/eureka/
    fetch-registry: true
    register-with-eureka: true