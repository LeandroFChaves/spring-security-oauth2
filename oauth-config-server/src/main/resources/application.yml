server:
  port: 8085
  servlet:
    context-path: /config-server

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/LeandroFChaves/spring-security-oauth2.git
          search-paths:
                        - oauth-configs